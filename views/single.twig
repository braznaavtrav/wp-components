{% extends 'base.twig' %}

{% block content %}
  {% if posts %}
    {% for post in posts %}
      {{ post.title }}
      <palette-canvas></palette-canvas>
      <script>
      var sections = {{ post.get_field('_cmb_component_canvas') }};
      </script>
    {% endfor %}
  {% endif %}
{% endblock %}